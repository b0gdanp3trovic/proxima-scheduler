apiVersion: v1
kind: Service
metadata:
  name: chronograf-service
  namespace: influx
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
      nodePort: 32001
  selector:
    app: chronograf
